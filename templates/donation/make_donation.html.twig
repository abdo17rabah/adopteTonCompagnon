{% extends "base.html.twig" %}

{% block body %}
    <div class="container py-5">
        <h1 class="h3 mb-3 font-weight-normal">Donation</h1>
        <div class="row">
            {{ form_start(form) }}
            {{ form_row(form.sum) }}
            {{ form_row(form.Donate,{'attr': {'class': 'btn btn-info mt-3'}})}}
            {{ form_end(form) }}
        </div>

    </div>

{% endblock %}